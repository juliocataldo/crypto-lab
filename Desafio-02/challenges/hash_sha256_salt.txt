===============================================
DESAFIO 2: SHA-256 com Salt (Medium)
===============================================

Salt (fornecido):
abc123xyz

Hash SHA-256 de sha256(salt + senha):
c701b165cb08f8d2d9bb77f07b40883fd72b013a31d1c1dadc49b1f4733dedf9

100% offline — sem precisar de internet (bash puro, testa cada candidata da wordlist):
  HASH_ALVO="<cole o hash acima>"
  for w in $(cat wordlist_comum.txt); do
    [ "$(echo -n "abc123xyz$w" | sha256sum | cut -d" " -f1)" = "$HASH_ALVO" ] && echo "ACHOU: $w"
  done

Verificacao manual de uma candidata:
  echo -n "abc123xyz<candidata>" | sha256sum

A flag e FLAG{<senha_original>}
